import { Paper } from "@mui/material";
import React from "react";

const EventContainer = ({ name, bgImage, hoyolabLink, startDate, endDate }) => {
  return;
  <Paper elevation={3}></Paper>;
};

export default EventContainer;
